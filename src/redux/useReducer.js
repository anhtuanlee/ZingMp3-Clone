const initState = {
    controls: {
        play: '',
        pause: '',
        next: '',
        prev: '',
        replay: '',
        random: '',
    },
    musicFavorite: {},
};

export const rootReducer = (state = initState , action) => {
    switch(action.type) {
        
    }
} 
